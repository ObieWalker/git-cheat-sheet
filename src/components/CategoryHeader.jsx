import React from 'react';

const CategoryHeader = ({categoryTitle}) => {
  return (
      <div>
        <h4 className="white-text">{categoryTitle}</h4>
      </div>
    );
  }

export default CategoryHeader;
